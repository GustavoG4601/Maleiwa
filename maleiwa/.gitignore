node_modules/
.env

# Ignorar imágenes subidas localmente (en Railway estas van al Volume)
store/uploads/*

# NO ignoramos los archivos .json de datos:
# Son la semilla inicial que se copia al Volume en el primer deploy.
# Una vez copiados, Railway solo lee/escribe del Volume y el repo no los pisa.
